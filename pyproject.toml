[project]
name = "asu-wind-collector"
version = "0.1.0"
description = "TCP server that collects raw data from embedded systems and logs to SQLite"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "watchfiles>=1.2.0",
]


